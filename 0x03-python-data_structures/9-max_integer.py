#!/usr/bin/python3
def max_integer(my_list=[]):
    large_number = my_list[0]
    for number in my_list:
        if len(my_list == 0:
            my_list = 0, "None"
        if number > large-number:
            large_number = number
        return large_number
